namespace TeleChat.WebUI.Pages;

public partial class Home
{
    private readonly List<int> _groups = Enumerable.Range(1, 10).ToList();
    private readonly List<int> _contacts = Enumerable.Range(1, 1).ToList();

}